/**
 * Vite Environment Type Declarations
 * 
 * This file provides TypeScript type definitions for the Vite development environment.
 * It includes:
 * - Type declarations for Vite's client-side features
 * - Module augmentation for Vite-specific imports
 * - Type definitions for Vite's environment variables
 * - Type support for Vite's asset imports
 * 
 * The triple-slash directive below imports Vite's client-side type definitions,
 * which are essential for proper TypeScript support when using Vite features like:
 * - Importing static assets (images, CSS, etc.)
 * - Using environment variables
 * - Hot Module Replacement (HMR)
 * - Vite-specific module imports
 */

/// <reference types="vite/client" />
